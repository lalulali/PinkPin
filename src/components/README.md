# Components

This directory contains React components for the Pink Pin Merchant App.

## Structure

- UI components (buttons, forms, cards, etc.)
- Layout components (Header, Sidebar, MainLayout)
- Feature components (MapPanel, FormAccordion, OrderCard, etc.)
